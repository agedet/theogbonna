export const TOKEN = {
    // Enbros/Partner use "accessToken" cookie (set by backend via Set-Cookie header)
    ACCESS_TOKEN: "accessToken",
  
    // Members use "memberAccessToken" cookie (set by backend via Set-Cookie header)
    MEMBER_ACCESS_TOKEN: "memberAccessToken",
  
    SESSION_TOKEN: "ogbonnas_session_token",
  
    EMAIL: "ogbonnas_email",
  
    ID: "ogbonnas_user_id",
  
    ROLE: "ogbonnas_user_role",
  };
  